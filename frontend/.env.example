# Optional: Set this to your deployed backend API URL when deploying to Vercel
# Example: VITE_API_URL=https://your-backend.railway.app/api
# If left unset or blank, the frontend defaults to relative '/api' (useful with Vercel rewrites or local proxy)
VITE_API_URL=
