PORT=5000
NODE_ENV=production

# MySQL 8.0 Connection Parameters (Local or Cloud e.g. Aiven, Railway, AWS RDS, PlanetScale, TiDB)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_secure_password
DB_NAME=metrix_r76

# JWT Secret & Token Duration
JWT_SECRET=METRIX_R76_DOCA_SECRET_KEY_2026_LEGAL_METROLOGY_COMPLIANCE
JWT_EXPIRES_IN=7d

# File Upload Directory
UPLOAD_DIR=uploads

# Allowed Frontend Origins (Use * or your Vercel URL e.g. https://your-project.vercel.app)
CORS_ORIGIN=*
